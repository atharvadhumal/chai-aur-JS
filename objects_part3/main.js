const course = {
    courseName: "JS in Hindi",
    Price: 999,
    courseTeacher: "hitesh",
}
const {courseTeacher: coach} = course //we have named the courseTeacher as coach here it will print the same value
console.log(coach)